# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# Base de datos de geolocalización (scripts/update-geolite2.sh) — ~70MB,
# se descarga con la license key de MaxMind, no va en git.
/data/GeoLite2-City.mmdb

# uploads (fotos de productos, comprobantes) - solo local en dev
/public/uploads/*
!/public/uploads/.gitkeep

# backups de la base (ver src/lib/backup.ts) - solo local/VPS, nunca en git
/data/backups/*
!/data/backups/.gitkeep
